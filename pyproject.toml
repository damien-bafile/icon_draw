[project]
name = "icon-draw"
version = "0.1.0"
description = "5x7 mono font character drawing tool"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
icon-draw = "src.main:main"

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "ruff>=0.15.17",
]
